{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug via CMake",
      "type": "cppvsdbg", // or "lldb" on macOS, "gdb" on Linux
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "preLaunchTask": "CMake: build"
    }
  ]
}